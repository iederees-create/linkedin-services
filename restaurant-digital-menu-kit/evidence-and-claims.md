# Evidence and claims

What is actually verified and may be claimed:
- The editor, TV display, mobile menu and print layout are working software, verified end-to-end via real `file://` loading (not a dev server) with seeded test data — confirmed all three output views correctly reflect editor changes, zero console errors.
- Three original themes exist and render distinctly (café/takeaway/food truck).
- An offline QR code encoder is included and structurally verified (correct finder-pattern placement for a real URL); it has not been scan-tested with a physical phone in this environment — recommend doing so before any client-facing claim of "scan-tested."
- A real Etsy draft exists for the DIY kit (listing ID 4576445855, verified against the live Etsy API via the deployment script's own verification step and independently confirmed in the production database).

What must NOT be claimed without further verification:
- No real client projects, reviews, sales figures or performance outcomes exist yet — do not reference any.
- Do not claim "scan-tested" QR codes until manually verified with a phone.
- Do not claim hardware/signage-platform integrations, ordering system support, or remote screen management — none of these are built.
- Do not claim the portfolio demo/project page, blog post or marketing video are live until those sections are actually completed and deployed (see project status).
